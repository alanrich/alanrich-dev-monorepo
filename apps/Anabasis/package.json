{
  "name": "anabasis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "nx serve anabasis",
    "build": "nx build anabasis",
    "build:client": "cd src/client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.1",
    "mongoose": "^7.6.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  }
}
